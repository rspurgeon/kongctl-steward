# Working requirements compatible with chromadb
# IMPORTANT: Use Python 3.9, 3.10, or 3.11 (NOT 3.12 or 3.14)

# Core dependencies
python-dotenv==1.0.0

# Using pydantic v1 to be compatible with chromadb 0.3.29
pydantic==1.10.13

# GitHub API
PyGithub==2.1.1

# LLM Providers (compatible with pydantic v1)
# Note: anthropic 0.40.0+ requires pydantic v1, works with chromadb
anthropic>=0.40.0,<1.0.0
openai>=1.3.0,<2.0.0

# Vector Database (requires pydantic<2.0)
chromadb==0.3.29

# Utilities
requests==2.31.0
tenacity==8.2.3
rich==13.7.0
click==8.1.7

# Note: If installation still fails on Python 3.14, use Python 3.11:
#   rm -rf venv
#   python3.11 -m venv venv
#   source venv/bin/activate
